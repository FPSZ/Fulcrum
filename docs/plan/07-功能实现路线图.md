# 枢衡 Fulcrum · 功能实现路线图(冲刺清单)

> 定位:**接下来一步步要做什么**的可勾选执行清单,按"对拿奖的边际收益"排优先级。
> 与 [02-MVP](02-MVP.md)(M1 技术规格)、[03-进度看板](03-进度看板.md)(状态真源)互补:本文回答**顺序与取舍**,看板回答**做到哪了**。
> 状态:草稿 v1.1 · 2026-06-15 创建,2026-06-18 对账。任何一项完成后,先勾这里、再同步看板对应行。
> 对账口径:本文勾选与 [03-进度看板](03-进度看板.md) 一致;看板是状态单一真源,逐 PR 细节见看板修订记录。

---

## 0. 排序依据(为什么是这个顺序)

赛题评分:**实际效果 30% · 技术创新 25% · 方案完整性 20% · 应用价值 20% · 展示表达 5%**。
关键约束:**9/15 24:00 截止**,交付 = 技术报告 PDF(≤30 页)+ 代码仓库 + 演示视频(≤10min)+ 报名表。

三条铁律决定优先级:

1. **30% 的"实际效果"= 量化指标**(攻击识别准确率、误报漏报、阻断率、审计完整性)。这些数字**只能由"真实防护闭环 + 评测引擎"产出**。所以 P0 不是控制台、不是占位页,而是**把后端桩换成真实现并跑出数字**。
2. **能演示 > 功能多**。评委看的是 10min 视频 + 现场答辩。一条"恶意文档→高危工具→阻断→审计可溯源→评测出分"的完整链路,胜过十个半成品页面。
3. **四大赛题目标要都覆盖到**(方案完整性 20%),但**深度优先于广度**:目标①②(识别+管控)做深拿"实际效果",目标④(评测审计)是出分载体必做,目标③(供应链)做**最小可信闭环**即可覆盖面。

> 一句话:**先让它真的拦得住、并能给出分数,再让控制台把这件事讲好看,最后补供应链与创新点。**

---

## P0 ── 端到端防护闭环 + 出分(命脉)

> 对应赛题目标 ①②④ · 评分:实际效果 30% + 技术创新 25% 的地基。
> 完成标志:`pytest` 全绿 +「跑一条命令,打印 Baseline vs Fulcrum 的 ASR/FPR/阻断率/审计完整率/溯源命中率」。
> 做法:**填 `src/fulcrum/capabilities/` 既有插槽,不改核心管线**(对称于前端插件系统)。

### P0-a 防护内核最小真链路(把桩换成真实现)

- [x] 确认/补齐领域类型:`SourceSpan`/`ToolIntent`/`Attribution`/`Finding`/`PolicyDecision`/`AuditEvent` 已完整(`core/domain/models.py`)
- [x] **输入检测**:`capabilities/detectors/keyword_rules.py` 升级为多源(用户/文档/网页/检索/记忆/工具返回)六类风险(注入/越狱/外发/敏感文件/命令执行/投毒)+ 来源信任级/直接间接加权 + low~critical 分级
- [x] **来源标注**:`labelers/role_trust.py`(`role_trust`)真实现——按角色信任归类 + 嵌入来源抽取(`---BEGIN/END---` 等定界,激活检测器间接来源加权);passthrough 保留为最小桩,已接入主管线
- [x] **归因**:新增 `capabilities/attribution/evidence.py`(`evidence`)——参数片段原文出现在来源摘录 → `derived_from_sources` + `置信度=来源信任权重×匹配强度`;`zero` 保留作 baseline
- [x] **工具风险评分**:新增 `capabilities/toolguard/heuristic.py`(`heuristic`)+ 共享纯函数 `toolguard/argrisk.py`(敏感路径/越界/危险命令/外联域名/裸IP);`zero` 保留作 baseline
- [x] **策略引擎**:新增 `capabilities/policy/yaml_policy.py`(`yaml`)+ `data/policies/default.yml`,支持条件 `tool_in/tool_name/source_trust/risk_at_least/attribution_at_least/risk_level/path_sensitive/path_outside_workspace/domain_allowed/command_dangerous` → `allow/sanitize/approve/block`,未知条件加载期报错;`allow_all` 保留作 baseline
- [x] **审计链 hash-chain**:`adapters/audit/memory_sink.py` 已正确实现 `event_hash=sha256(prev_hash+canonical_json)`(进程内);**SQLite 持久化待办**(接口不变,P1 随后端落库时替换)
- [x] 装配:`config/fulcrum.yml` 切到真实现(detector=keyword_rules / attributor=evidence / risk_scorer=heuristic / policy=yaml);核心管线零改动
- [x] 单测:detector(5)/ risk_scorer(5)/ attribution(3)/ policy(8)/ 集成拦截+审计(2)—— 全绿,import-linter 分层契约保持

### P0-b 工具网关与受控执行

- [x] 受控文件工具 `file.read` / `file.write`(`capabilities/tools/file_tools.py`,工作区边界纵深防御);`http.request` / `shell.exec` 暂未做真执行体(demo 中均被 block/approve 拦在执行前,P0-d/P1 再补真执行)
- [x] 执行边界:工作区限定在工具内 + 策略 `path_outside_workspace`/`domain_allowed`/`command_dangerous` 双层;真沙箱(容器/受限子进程)归 P3
- [x] 集成验证:正常 file.read 放行并执行 · 越权写入被 block · shell.exec 走 approve(`tests/test_block_flow.py` + demo 实跑)
- [ ] Tool Gateway `POST /tools/call`:端点已存在于 `adapters/api/`,待接控制台(P1)

### P0-c LLM 代理 + demo Agent(让闭环变成"政务场景")✅ 真模型已接通

- [x] **真实模型适配器** `adapters/model/openai_client.py`(`openai`):连任意 OpenAI 兼容端点(已接 **MiMo 小米**),声明 4 受控工具、function-call 映射回 `ToolCall`;密钥经 `.env` 注入
- [x] 政务办公 Agent demo(`fulcrum/demo`):`uv run python -m fulcrum.demo` —— MiMo 当大脑,工具调用全程穿过枢衡,打印 检测→归因→策略→处置→审计 全过程
- [x] 4 条演示链路:正常读取(ALLOW+执行)· 越权写入(BLOCK)· 系统命令(APPROVE)· 恶意文档间接注入(目标①输入检测全 critical)
- [ ] 多轮补全:把工具执行结果回填给模型产出最终答复(需 Message 带 tool_call_id;留作增强)
- [x] 来源标注细化:`role_trust` 按真实来源类型/角色分级(嵌入来源抽取 + 标记块定界);passthrough 降为最小桩

### P0-d 评测引擎 + 中文样例集(出分,实际效果 30% 的载体)✅ 已落地

- [x] 评测入口:`python -m fulcrum.eval [--dataset 样例集.jsonl] [--out 报告.json]` → 控制台打印 P0 主结果表 + 攻击类型分桶,逐样例明细写 `docs/eval/results/latest.json`
- [x] 中文脱敏样例集(≥20 条):`samples/eval/govoffice.jsonl` 23 条(direct/indirect 注入 · jailbreak · knowledge_poisoning · unauthorized_tool · benign)+ `samples/eval/govoffice-egress.jsonl` 5 条出口级(`screen_output` 回复防泄露)
- [x] Baseline 对照:主结果表同时给 Baseline(ASR 100%)与 Fulcrum(ASR 0%)两列对照
- [x] 核心指标产出:ASR 降幅 / Recall(BSR) / FPR / Utility / 高危动作处置正确率 / 审计完整率 / hash-chain 通过率(实测全部达标);**Source Hit@1·@3 待补**(需工具级归因金标准)
- [x] 对照 [eval/00 评测指标体系](../eval/00-评测指标体系.md) 校准口径

> ✅ **P0 验收**(也是演示视频骨架):①正常任务不误伤 ②恶意文档高危调用被阻断 ③审计详情可说清"哪段输入→哪次调用→哪条策略→什么处置" ④评测报告展示 Baseline 与 Fulcrum 的 ASR 差异。

---

## P1 ── 让控制台把故事讲好看(应用价值 + 展示)

> 对应赛题目标 ①②④ 的可视化 · 评分:应用价值 20% + 展示表达 5%。
> 完成标志:控制台不再靠备份 mock,**真实读后端**;占位页全部接真数据。
> 前置:P0 跑通、有真实数据可读。

- [x] **OpenAPI 契约**:`python -m fulcrum.openapi` 导出 `docs/api/openapi.json`(30 路径,无副作用),前端据此对接,走 TanStack Query
- [x] **审计溯源页**:接 `GET /audit` 审计链,展示 hash-chain + 篡改标记 + 派生情景(复用事件页 master-detail)
- [x] **评测验证页**:接 `GET /eval/report`,展示 P0 主结果表 + 逐样例(有真用真,否则回退演示 seed)
- [x] **策略中心页**:接 `GET /policies`,可视化当前装配策略规则与条件(只读)
- [x] **工具网关页**:接 `GET /tools/calls`,展示工具调用治理流水 + 处置(allow/approve/block/sanitize)
- [x] **实时面板接后端**:七张业务页经 TanStack Query 轮询接真(15~60s)替换本地模拟;SSE/WebSocket 推送留作增强
- [ ] 第二种备份资源(策略 policy)接导入,验证"不止事件"的资源扩展

---

## P2 ── 供应链最小可信闭环(覆盖赛题目标③)

> 对应赛题目标 ③ · 评分:方案完整性 20%(覆盖面)。
> 完成标志:能扫一个恶意 MCP/Skill manifest 并给出 `allow/review/block` 评级 + 控制台可见。

- [x] 扫描命令:`python -m fulcrum.scan`(`capabilities/supplychain/manifest_scanner.py` → 真实现,评级即退出码)
- [x] 检测项:声明权限 · 可疑描述(提示注入)· 外联端点 · 依赖来源 · **安装期钩子(postinstall)** → 分级评级
- [x] 脱敏样例:`samples/supplychain/` 良性(weather-skill)+ 恶意(exfil-plugin),可扩;纳入评测集算召回率待补
- [x] 供应链页:接 `GET /supply/scans` 展示扫描评级 + 风险明细(镜像 ScanReport)

> 范围克制:**不做**真实 IDA/Ghidra/沙箱逆向(保留为后续增强),只做 manifest/脚本元信息的可解释扫描。

---

## P3 ── 创新与加分项(技术创新 25%,挑性价比高的做)

> 对应技术创新 25% + 应用价值。**按时间余量取舍,不必全做**;每项都是独立加分点。

- [ ] **LLM-as-judge 增强**:在规则检测之上叠模型研判(提升注入识别 Recall、降低 FPR)——直接抬"实际效果"数字
- [x] **异常任务链阻断**:`sequence` 链分析做深为跨步数据流污点(外发参数确实携带上一步工具返回 → `chain.taint_exfiltration` critical 阻断;解码后比对抗编码规避),对应目标②"异常任务链阻断"
- [x] **RBAC 权限组**:用户管理页已落地(部门树 + 成员 + 自定义角色 + 申请审批),管控/审批落到角色闸门
- [ ] **AI 操作助手**:在 RBAC 闸门内的运营助手(先 RBAC 再助手)
- [x] **沙箱执行强化**:`echo` → `restricted` 受限执行器(路径/命令/外联边界 + 超时 + 出入参体积上限);真实进程/容器隔离仍属后续
- [x] **单容器部署**:`deploy/Dockerfile` + `docker-compose.yml`,一条命令起全部生产服务(详见 [04-单容器部署方案](04-单容器部署方案.md))

---

## P4 ── 交付物(9/15 截止,务必倒排期)

> 评分:展示表达 5%,但**缺交直接出局**;实际效果/完整性也靠这些材料呈现。

- [ ] **技术方案报告 PDF**(≤30 页):问题分析 / 技术路线 / 算法设计 / 实验方案 / 指标体系 / 预期效果(指标用 P0-d 真实数据)
- [ ] **演示视频**(≤10min):P0 验收四步 + 评测出分 + 供应链扫描
- [ ] **代码仓库复现说明**:运行说明 + 部署说明(`python -m ...` / Docker 一条命令)
- [ ] **补充材料**:脱敏样例集、评测脚本、攻击样例、审计日志样例
- [ ] **报名表**:报名系统审核通过(9/15 前,信息与系统一致)

---

## 待办池(小项/技术债,见缝插针)

- [ ] 前端路由级 `React.lazy` 分割 + vendor `manualChunks`(消 574kB 主 chunk 警告;功能页变多时再做)
- [ ] 备份加 `dataVersion` 字段,schema 变更时自动失效旧 localStorage 缓存
- [ ] 侧栏分组标签(监测/管控/取证/系统)收起时淡出
- [x] 备份导入文件大小上限(5MB,防误导入大文件卡死)— 已加于 `lib/backup/context.tsx`

---

## 修订记录

| 版本 | 日期       | 变更                                                         |
| ---- | ---------- | ------------------------------------------------------------ |
| v1   | 2026-06-15 | 初版冲刺路线图:按评分权重排 P0~P4,P0 锁定"端到端闭环+出分" |
| v1.1 | 2026-06-18 | 与看板对账勾选已落地项:P0-d 评测引擎+样例+Baseline+指标(出分载体)✅;P0-a/P0-c 来源标注 `role_trust` 真实现 ✅;P1 OpenAPI 契约 + 审计/评测/策略/工具网关四页接真 + 实时面板轮询接真 ✅;P2 供应链最小闭环(`fulcrum.scan` + manifest 扫描 + 供应链页)✅;P3 异常任务链阻断(跨步污点)、RBAC、受限执行器、单容器部署 ✅。检测/出口/评测面加固一轮(PR #37~#52)逐 PR 细节见看板 v1.34。余 P3:LLM-as-judge、AI 操作助手、真实进程/容器沙箱;余 P4 交付物 |
