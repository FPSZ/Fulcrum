# ───────────────────────────── Secrets & Local Config ─────────────────────────────
.env
.env.*
!.env.example
*.local
secrets/
*.pem
*.key
*.crt
credentials.json
docs/secrets/
# ───────────────────────────── Python ─────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
# 例外:前端 console/src/lib 是真实源码(utils / motion / backup / module),不受上面 Python `lib/` 规则约束
!console/src/lib/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
.venv/
venv/
env/
ENV/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
.tox/

# ───────────────────────────── Node / Frontend ─────────────────────────────
node_modules/
.pnpm-store/
dist-ssr/
*.tsbuildinfo
.next/
.nuxt/
.cache/
coverage/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ───────────────────────────── Data, Models & Artifacts ─────────────────────────────
# Large datasets / model weights should not live in git (use DVC / release assets)
data/raw/
data/interim/
data/runtime/
*.sqlite
*.sqlite3
docs/eval/results/
*.bin
*.gguf
*.safetensors
*.ckpt
*.onnx

# ───────────────────────────── Diagram / Render Tooling ─────────────────────────────
# 最终图与图源(diagrams/*.svg、*.d2)入库;
# 生图过程的临时产物(预览图、废弃图源、渲染缓存/配置)一律不入库。
**/diagrams/_scratch/
_preview.png
*.preview.png
.puppeteer.json
pconf.json
.mermaid/
.mermaid-cli/
puppeteer-cache/
.cache/puppeteer/
ms-playwright/
.local-chromium/
render.log

# ───────────────────────────── Logs & Runtime ─────────────────────────────
logs/
*.log
*.pid
tmp/
temp/
shot.json

# ───────────────────────────── IDE / Editor / OS ─────────────────────────────
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.shared.json
*.swp
*.swo
.DS_Store
Thumbs.db
desktop.ini
# 早期静态原型归档目录(仅根目录);不写成裸 `try` 以免忽略任意层级的 try/ 目录。
/try/

# ───────────────────────────── Report Generation Artifacts ─────────────────────────────
# 技术方案报告的中间稿、预览图和生成脚本不进入版本库；正式交付稿单独保留。
_rev_*.json
docs/report/preview/
docs/report/images/
docs/report/professional-style.css
docs/report/技术方案报告-v4.md
docs/pdf/*
!docs/pdf/
!docs/pdf/Fulcrum-技术方案报告-v4-final6.docx
scripts/create_word_template.py
scripts/generate_pdf_fpdf.py
scripts/generate_report_pdf.py
scripts/postprocess_docx.py
scripts/preview_report.py
scripts/update_toc.py

# 报告制作过程资产(2026-09-10 追加)
# 均保留在本地磁盘、未删除;定稿需要入库时用 `git add -f <路径>` 显式添加。
# 截图与视频素材:合计约 10MB 二进制,可由运行中的控制台重新截取,不进版本库。
docs/report/photo/
docs/report/视频素材/
# 报告过程稿:封面/截图/Logo 方案等待定材料,正式交付稿另行入库。
docs/report/02-报告审阅与封面建议.md
docs/report/03-截图方案.md
docs/report/04-截图环境与拍摄清单.md
docs/report/05-Logo候选方案.md
# 一次性资产渲染脚本(封面/流程图/视频卡片/品牌 Logo)。
scripts/render_report_*.py
scripts/render_video_cards.py
scripts/render_brand_logo*.mjs
