# 枢衡 Fulcrum · AGENTS.md

> 本文件是本仓库的长期协作约定,适用于 AI 助手、自动化工具和人类协作者。
> 它只写稳定规则,不写短期计划、临时阶段目标或某一版实现细节。

修改仓库前先读本文件。若本文件与当前任务冲突,先说明冲突并与维护者确认,不要自行绕过。

---

## 1. 项目定位

**枢衡(Fulcrum)** 是面向政企场景的大模型智能体安全项目,目标是在智能体外侧提供安全管控、评测验证、审计溯源与运行治理能力。

项目长期关注:

- 多源输入风险:提示注入、间接指令污染、越狱诱导、知识投毒等。
- 工具调用风险:文件读写、系统命令、浏览器访问、外部接口调用等。
- 扩展组件风险:插件、脚本、Skill、MCP 工具等供应链问题。
- 过程治理风险:缺少监测、审计、溯源、复现与分级处置。

关键入口:

- [README.md](README.md):项目门面与导航。
- [docs/plan/](docs/plan/):项目定位、范围、路线。
- [docs/arch/](docs/arch/):技术架构、模块边界、接口契约。
- [docs/eval/](docs/eval/):评测方案、指标口径、实验记录。
- [docs/reference/](docs/reference/):赛题资料、外部参考、调研材料。

---

## 2. 工作原则

1. **先理解,后修改**  
   先读相关文档和现有内容,不要跳过上下文直接改。

2. **结构先于内容**  
   文件放到正确目录。根目录只保留长期入口文件。

3. **文档即资产**  
   重要背景、决策、接口、指标、实验结论必须落到文档,不要只存在对话里。

4. **可复现是底线**  
   涉及效果、实验、评测、演示的结论,必须说明数据、步骤、环境或脚本。

5. **小步修改**  
   一次改动只解决一个清晰问题。避免把无关重构、格式化和内容改动混在一起。

6. **安全优先**  
   本项目研究安全问题,仓库自身必须严格避免泄密、误用攻击样例和引入不可信组件。

---

## 3. 目录约定

仓库目录应保持清晰、自解释。新增目录前先确认其长期职责。

```text
Fulcrum/
├── README.md              项目门面与导航
├── AGENTS.md              仓库协作约定
├── .gitignore             忽略规则
├── docs/                  文档资产
│   ├── plan/              项目定位、范围、路线、里程碑
│   ├── arch/              架构、模块设计、接口契约
│   ├── eval/              评测方案、指标定义、实验记录
│   └── reference/         赛题资料、外部参考、调研材料
├── src/                   后端源代码
├── console/               前端控制台(React + TS 工程)
├── tests/                 测试
├── scripts/               可复现运行、部署、评测脚本
├── deploy/                部署相关资产
├── data/                  数据集说明与可入库小样本
└── samples/               脱敏攻击样例集
```

放置规则:

- 规划、定位、路线 -> `docs/plan/`
- 架构、模块、接口、策略契约 -> `docs/arch/`
- 指标、实验、评测结果 -> `docs/eval/`
- 原始资料、调研、外部参考 -> `docs/reference/`
- 可复现脚本 -> `scripts/`
- 测试代码 -> `tests/`
- 脱敏攻击样例 -> `samples/`
- 数据说明和小样本 -> `data/`

不要把临时笔记、下载物、调试脚本、导出报告、截图、实验产物散落在根目录。

---

## 4. 文档规范

- 默认使用中文;专有名词、协议名、代码标识符保留英文。
- 文档开头说明“本文回答什么问题”,并标注状态:草稿 / 评审中 / 定稿 vN。
- 重要文档维护“修订记录”。
- 使用绝对日期,例如 `2026-09-15`,不要写“明天”“下个月”。
- 文档引用使用相对路径 Markdown 链接。
- 区分事实、计划、假设和待验证结论。
- 未验证的效果不要写成确定结果。
- 文件名不要包含空格。

命名建议:

- `docs/plan/`、`docs/arch/`、`docs/eval/` 下使用 `NN-标题.md` 保持阅读顺序。
- 中文文档名允许,以清晰为先。
- 代码目录、代码文件、变量、函数使用英文。

---

## 5. 架构与评测约定

架构和评测内容应遵循以下长期原则:

- 先明确边界,再讨论实现。
- 先定义契约,再写代码。
- 先定义指标口径,再声称效果。
- 安全策略要说明输入、判定条件、处置动作和审计证据。
- 评测指标要说明公式、方向、数据集、样例标注和复现方式。
- 架构文档要说明取舍、失败模式、降级策略和未解决问题。

任何重大架构或评测口径变化,都应同步更新相关 `docs/arch/` 或 `docs/eval/` 文档。

---

## 6. 代码与脚本约定

具体技术栈以架构文档和工程配置为准。本文件只规定通用要求:

- 新增模块要有清晰入口、职责边界和最小运行说明。
- 外部输入必须校验。
- 配置与密钥分离:`.env` 不入库,`.env.example` 可入库。
- 对外接口、策略格式、样例 schema 等要有文档说明。
- 自动化脚本应可重复运行,避免依赖个人机器状态。
- 脚本命名和参数要清晰,避免一次性黑盒脚本长期留存。
- 不要为了临时验证引入长期依赖;确需引入时说明用途和影响。

---

## 7. 安全红线

绝不提交:

- 密钥、令牌、账号、Cookie、API Key、真实凭据。
- 真实政企数据、真实个人敏感信息、未脱敏日志。
- 可直接危害真实系统的攻击 payload。
- 大型模型权重、原始数据集、大文件下载物。
- 未审查来源的第三方二进制或脚本。

攻击样例要求:

- 仅用于评测、复现和演示。
- 必须脱敏,并标注用途、风险级别和预期处置。
- 不包含可直接攻击真实目标的细节。
- 高风险样例入库前必须先确认管理方式。

实验要求:

- 不对真实第三方系统做未授权测试。
- 高危工具调用必须在隔离环境或沙箱中执行。
- 外联行为必须可解释、可关闭、可审计。

---

## 8. Git 规范

提交信息使用中文,格式如下:

```text
<类型>: <一句话中文简述,不超过 50 字>

<中文正文:说明做了什么、为什么这么做、影响范围。>
```

常用类型:

| 类型 | 含义 |
|---|---|
| `feat` | 新功能 / 新能力 |
| `fix` | 缺陷修复 |
| `docs` | 文档 |
| `chore` | 工程杂务、配置、目录调整 |
| `refactor` | 重构,不改变外部行为 |
| `test` | 测试 |
| `exp` | 实验 / 评测记录 |

要求:

- 一次提交聚焦一件事。
- 正文说明动机和影响范围。
- 不提交临时产物、密钥、大文件或未脱敏样例。
- 未经明确同意不要 push 到远程。
- 不使用破坏性 Git 命令覆盖他人改动。

---

## 9. AI 助手工作方式

AI 助手在本仓库工作时:

1. 先判断任务属于文档、架构、评测、实现、实验还是安全样例。
2. 读取最小必要上下文,不要凭空假设。
3. 涉及方向、架构、技术栈、目录结构、安全样例时,先说明影响并等待确认。
4. 修改前说明将改什么;修改后说明改了什么、为什么、如何验证。
5. 发现文档冲突时,指出冲突并给出建议,不要静默选择。
6. 发现无关脏改动时,不要回滚;只处理当前任务相关内容。

回答要求:

- 默认中文。
- 直接、清晰、少空话。
- 给其他 AI 的提示词要能直接复制执行。
- 评价方案时优先指出风险、缺口和改进建议。

---

## 10. 交付前检查

完成非平凡改动前自查:

- 文件是否放在正确目录。
- 是否更新了相关文档、链接和修订记录。
- 是否混入临时文件、下载物、日志或敏感信息。
- 是否区分了事实、计划和待验证项。
- 若涉及指标,是否有公式、口径和复现方式。
- 若涉及架构,是否说明边界、取舍和失败模式。
- 若涉及代码或脚本,是否说明运行和验证方式。

---

## 11. 事实优先级

发生冲突时,优先级如下:

1. 用户当前明确指令。
2. 本文件 `AGENTS.md`。
3. 赛题原文或其提取文本。
4. `docs/plan/` 中的定稿文档。
5. `docs/arch/` 中的定稿文档。
6. `docs/eval/` 中的定稿文档。
7. 其它正式文档。
8. 草稿、调研、历史讨论和临时笔记。

冲突影响决策时,先提出问题,不要擅自改写事实。

---

## 修订记录

| 版本 | 日期 | 变更 |
|---|---|---|
| v1 | 2026-06-14 | 初稿:确立企业级开发原则、目录规范、文档规范、中文 commit 规范、安全红线、协作流程 |
| v2 | 2026-06-14 | 收敛为长期通用协作规范:移除阶段性计划和具体架构版本细节,保留稳定的目录、文档、安全、Git 与 AI 协作约定 |
