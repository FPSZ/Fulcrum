# 枢衡 Fulcrum · 开发指南(AGENTS.md)

> 本文件是**所有协作者(包括 AI 助手与人类开发者)在本仓库工作的统一约定**。
> 动手前先读本文件。任何与本文件冲突的操作都应先在此处达成共识、更新本文件,再执行。
> 适用对象:Claude Code / 其它 AI Agent / 团队成员。

---

## 0. 一句话项目背景

**枢衡(Fulcrum)** 是面向政企场景的大模型智能体「全生命周期安全中台」,旁挂在智能体外侧管控"输入→决策→工具调用→执行"全过程,并产出审计与评测证据。
完整背景见 [docs/plan/00-项目大方向.md](docs/plan/00-项目大方向.md)。这是一个**参赛项目(挑战杯 XA-202620),交付截止 2026-09-15**。

**当前阶段:规划期。** 大方向已定稿,技术架构尚未定。**未经讨论不要新建代码目录、不要引入技术栈。**

---

## 1. 核心开发原则

把这里当作一个**真正的企业级工作区**,不是临时文件夹。任何提交都要对得起"可复现、可审计、可交付"。

1. **结构先于代码** —— 目录结构必须清晰、自解释。文件放对位置,宁可不放,不要乱放。
2. **无垃圾入库** —— 临时脚本、实验产物、个人笔记、下载的样本、密钥、大文件一律不进 git(见 `.gitignore`)。
3. **先对齐,后动手** —— 大方向 → 技术架构 → 详细设计 → 编码,逐层确认。跨越层级前先确认。
4. **可复现是底线** —— 任何"实际效果"类结论必须能被他人按文档复现(这是评分 30% 的命脉)。
5. **文档即资产** —— 重要决策、接口契约、实验指标都要落到文档,不靠口头/记忆。
6. **小步提交** —— 一次提交只做一件事,提交信息说清楚"做了什么、为什么"。
7. **安全自觉** —— 本项目是安全项目;攻击样例、敏感数据要隔离管理,绝不误入主分支或公开。

---

## 2. 目录与文件组织规范

> 代码目录待架构定稿后建立,以下为**已生效**的规范与**预留**约定。

```
Fulcrum/
├── README.md              项目门面:是什么、能力、状态、导航
├── AGENTS.md              本文件:开发约定
├── .gitignore
├── docs/                  所有文档
│   ├── plan/              规划:大方向、路线图、里程碑   (编号前缀 00- 01- …)
│   ├── reference/         原始榜题资料、外部参考
│   ├── arch/   (预留)     技术架构、模块设计、接口契约
│   └── eval/   (预留)     评测方案、指标定义、实验记录
├── src/        (预留)     源代码(架构定稿后建,按模块分子目录)
├── tests/      (预留)     测试
├── data/       (预留)     数据集说明与小样本(大文件不入库,见 .gitignore)
├── scripts/    (预留)     可复现的运行/部署/评测脚本
└── samples/    (预留)     攻击样例集(需脱敏/隔离管理)
```

**放置规则**:

- 规划/设计/指标 → `docs/` 对应子目录,**不要散落在根目录**。
- `docs/plan/` 与 `docs/arch/` 文件用 `NN-标题.md` 数字前缀编号,保证阅读顺序。
- 临时产物、个人草稿 → **不入库**;需要共享的草稿放 `docs/` 并标注"草稿/WIP"。
- 不确定该放哪 → 先问,不要默认丢根目录。

---

## 3. 文档规范

- **语言:中文为主**(本项目面向中文评审与中文团队)。专有名词、代码标识符保留英文。
- 每篇规划/架构文档开头用一句话说明**本文回答什么问题**,并标注**状态**(草稿 / 评审中 / 定稿 vN)。
- 重要文档维护"**修订记录**"表(版本 / 日期 / 变更),已在 `00-项目大方向.md` 示范。
- 相对日期一律写成**绝对日期**(如"2026-09-15"而非"下个月")。
- 文件内引用其它文件用相对路径 markdown 链接,便于点击跳转。

---

## 4. 命名规范

- 文档文件名:中文允许,清晰为先;规划/架构加数字前缀 `NN-`。
- 代码标识符(目录、文件、变量、函数):**英文**,遵循所选语言的社区惯例(架构定稿时在本节补充具体风格)。
- 分支、commit、issue:见第 5 节。
- 禁止文件名出现空格;中文文件名可用,但代码相关一律英文。

---

## 5. Git 工作流与提交规范

### 5.1 提交信息(Commit Message)—— 必须用详细中文

格式:

```
<类型>: <一句话中文简述,不超过 50 字>
<空行>
<中文正文:说明做了什么、为什么这么做、影响范围。可分条列出。>
```

**类型(type)**:

| 类型         | 含义                                          |
| ------------ | --------------------------------------------- |
| `feat`     | 新功能 / 新能力                               |
| `fix`      | 缺陷修复                                      |
| `docs`     | 文档(规划、说明、注释)                        |
| `chore`    | 工程杂务(配置、依赖、目录调整、.gitignore 等) |
| `refactor` | 重构(不改外部行为)                            |
| `test`     | 测试                                          |
| `exp`      | 实验 / 评测记录                               |

**要求**:

- 标题用中文简述,正文用中文把"动机与影响"讲清楚,**不写一句话流水账**。
- 一次提交聚焦一件事;混杂改动要拆分。
- 不提交密钥、大文件、攻击样本原始数据。

示例:

```
docs: 确定项目大方向并搭建企业级仓库骨架

- 新增 docs/plan/00-项目大方向.md,定稿"做什么":定位为智能体全生命周期
  安全中台,范围采用一体化闭环+深做目标1/2,主场景选政务办公助手。
- 新增 README.md 与 AGENTS.md,明确项目门面与协作约定。
- 配置企业级 .gitignore;将原始榜题资料归位到 docs/reference/。
```

### 5.2 分支策略

- `main`:始终保持可交付、可复现的稳定状态。
- 功能/文档开发开 `feat/xxx`、`docs/xxx` 等分支,完成后合并回 `main`。
- 规划期初始骨架可直接提交 `main`;进入编码期后,改动走分支 + 合并。

### 5.3 推送

- **未经明确同意不擅自 push 到远程**;commit 可按约定进行。

---

## 6. 代码规范(架构定稿后补全)

> 占位:技术栈未定。架构确定后,本节补充语言版本、格式化工具(如 ruff/black/eslint/prettier)、
> lint 规则、类型检查、测试框架、依赖管理方式、最小可运行命令(`make` / 脚本)。

**先行通用约定**:

- 任何模块都要有最小可运行入口与 README/运行说明,保证"克隆即可复现"。
- 配置与密钥分离:用 `.env`(不入库)+ `.env.example`(入库,占位)。
- 对外接口(尤其安全策略、评测指标)要有明确契约文档,放 `docs/arch/`。

---

## 7. 安全红线(本项目尤其重要)

- 🚫 **不提交**:任何密钥、令牌、真实凭据、被保护系统的真实数据。
- 🚫 **攻击样例隔离**:`samples/` 下的攻击 payload 需脱敏、标注用途,仅用于评测;不得包含可直接危害真实系统的内容。
- 🚫 **不引入未审查的第三方组件**(本项目本身就在做供应链安全,自己更要以身作则)。
- ✅ 评测/演示在**隔离环境**进行;高危工具调用在沙箱内执行。

---

## 8. 协作流程(给 AI 助手的执行约定)

1. 接到任务先判断属于哪一层(大方向 / 架构 / 实现),**不越层**。
2. 涉及方向性、技术选型、目录结构的决策,**先确认再动手**。
3. 改动后,在回复里说明:改了什么、为什么、是否影响既有约定。
4. 需要 commit 时用第 5 节的中文规范;**push 前必须征得同意**。
5. 发现本文件与现状不符,先更新本文件再继续。

---

## 修订记录

| 版本 | 日期       | 变更                                                                              |
| ---- | ---------- | --------------------------------------------------------------------------------- |
| v1   | 2026-06-14 | 初稿:确立企业级开发原则、目录规范、文档规范、中文 commit 规范、安全红线、协作流程 |
